/**
 * Parsers, layouts e normalizadores puros dos dois layouts do MVP (TDS 9.3; Implementation
 * Plan M5/DR-1). Nenhuma classe aqui consulta banco ou depende de Spring — o
 * {@link dev.fincore.ingestion.parser.ParseContext} traz tudo que vem de {@code Source}.
 */
package dev.fincore.ingestion.parser;
